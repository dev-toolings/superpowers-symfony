---
description: Voters, access control, rate limiting, form validation and input handling.
globs: ["src/Security/**","src/Voter/**","src/Form/**","config/packages/security.yaml"]
alwaysApply: false
---

<!-- @generated by scripts/build.ts from content/ — DO NOT EDIT -->
# Security

Voters, access control, rate limiting, form validation and input handling.

Skills are instructions, not documentation. When a task matches one, open its
`SKILL.md` and follow it literally — then open the sibling `reference.md` only
if you need the deep implementation detail.

- **api-platform-security** — Secure API Platform resources with security expressions, voters, securityPostValidation, and operation-level access control
  Open `content/skills/api-platform-security/SKILL.md`.
- **form-types-validation** — Build Symfony forms with custom Form Types, validation constraints, HTTP 422 handling, and multi-step flows
  Open `content/skills/form-types-validation/SKILL.md`.
- **rate-limiting** — Implement rate limiting with the Symfony RateLimiter (sliding window, token bucket, fixed window) and the #[RateLimit] controller attribute
  Open `content/skills/rate-limiting/SKILL.md`.
- **symfony-voters** — Implement granular authorization with Symfony Voters; decouple permission logic from controllers; test authorization separately
  Open `content/skills/symfony-voters/SKILL.md`.
